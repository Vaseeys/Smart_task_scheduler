# Smart_task_scheduler
A high-performance Task Scheduling System built in C++ using a Binary Max-Heap Priority Queue. Features persistent file storage, O(\log n) efficiency, and dynamic priority assignment to ensure critical tasks never wait in line. Optimized for resource-heavy environments.
